//  import libraries
import React from 'react';
import { Text } from 'react-native';

const headerText = 'more header text';
//  Make component
const Header = () => {
  return <Text>{headerText}</Text>;
};

//  Make component available to other parts of the app
export default Header;
